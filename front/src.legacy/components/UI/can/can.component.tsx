import React, { ReactNode } from "react";

type Props = {
  children: ReactNode;
  action: string;
  object: string;
};

const Can: React.FC<Props> = ({ children, action, object }) => {
  return null;
};

export default Can;
